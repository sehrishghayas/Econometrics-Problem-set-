##Distributions , samples 
set.seed(1)
rnorm(500)
rpois(500, 1)
rpois(500, 2)
set.seed(10)
rbinom(500, 1, 0.5)
set.seed(1)
sample(rnorm(500),0:1 ,)
sample (rpois(500, 2), 1:4)
sample (rpois(500, 2), 1:4)
sample(rnorm(500),0:1)
sample(rnorm(500),1:3)
sample(1:10,4)
sample (1:20,5)
mean(rbinom(500, 1, 0.5))
mean(rpois(500, 2))
mean(sample (rpois(500, 2), 1:4))
mean(sample(rnorm(500),1:3))
runif(500,1, 10)
rpois(500, 2)
sample(runif(500,1, 10))
sample(rpois(500, 2))
mean(sample(runif(500,1, 10)))
mean(sample(rpois(500, 2)))

##Part C Hypothesis testing 

library(data.table)
t.test(dt.wages)
dt.nonwhite<- (dt.wages)
t.test(dt.wages[wage==1, female], dt.wages [wage==0, female]) 
t . test ( dt.wages[ female == 1, wage] , dt .wages[ female == 0, wage] )
dt.female <- (dt.wages)
dt.wages(, c(female))
library(data.table)
dt.wages [t . test (wage ~ nonwhite)]
